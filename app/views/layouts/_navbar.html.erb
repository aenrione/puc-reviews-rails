<nav class="navbar navbar-expand-lg navbar-light bg-primary">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item" active>
        <a class="nav-link" href="<%= root_path %>">Inicio <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= teacher_reviews_path %>">Reseñas Profesores</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= course_reviews_path %>">Reseñas Cursos</a>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Explorar
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href="<%= teachers_path %>">Profesores</a>
          <a class="dropdown-item" href="<%= courses_path %>">Cursos</a>
        </div>
      </li>
    </ul>
    <% if user_signed_in? %>
    <span class="nav-item">
      <button class="btn btn-success">+ Reseña Prof</button>
    </span>

    <span class="nav-item ml-1">
      <button class="btn btn-secondary">+ Reseña Curso</button>
    </span>

    <span class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <%= current_user.name %>
      </a>
      <div class="dropdown-menu" aria-labelledby="navbarDropdown">
        <a class="dropdown-item" href="<%= user_path(current_user) %>">Editar Perfil</a>
        <%= button_to  "Cerrar Sesión", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
      </div>
    </span>
    <% else %>
    <span class="nav-item">
      <%= button_to  "Registrarse", new_user_registration_path, method: :get, class: "btn btn-success" %>
    </span>

    <span class="nav-item ml-1">
      <%= button_to  "Iniciar Sesión", new_user_session_path(User.new), class: "btn btn-secondary" %>
    </span>
    <% end %>
  </div>
</nav>